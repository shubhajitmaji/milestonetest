# order_processing.py

def create_order(order_id, product_name, quantity):
    # Function to create an order
    return f"Order {order_id} created for {quantity} of {product_name}"

def process_order(order_id):
    # Function to process an order
    return f"Order {order_id} processed"
